"""STUB for Person 2. Replace with trigger stamping and relabelling."""


def maybe_poison(x, y, spec, round, target_class, seed):
    """No-op if spec is None, mode is not "scale", or round < start_round."""
    return x, y
