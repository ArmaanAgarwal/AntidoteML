"""STUB for Person 1. Does nothing yet, so updates come back as zeros.

Replace with seeded SGD over permuted batches (step 4 of the Person 1 section).
"""


def local_train(model, x, y, epochs, batch_size, lr, momentum, seed, device):
    """Train the model in place. No return value."""
    return None
